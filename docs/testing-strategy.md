# Teststrategie

Unit Tests prüfen fachliche Regeln ohne Infrastruktur. Integrationstests starten die API über `WebApplicationFactory`. In CI werden Restore, Build und Tests bei jedem Push und Pull Request ausgeführt.
