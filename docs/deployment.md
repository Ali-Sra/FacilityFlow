# Deployment

Für die lokale Demonstration stehen Dockerfiles und Docker Compose bereit. In einem produktiven Zielbild werden API und Web hinter einem TLS-Reverse-Proxy betrieben. Datenbank und Secrets werden außerhalb der Container verwaltet.
